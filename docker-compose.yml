version: '3.8'

services:
  # Service pour l'application Spring Boot
  app:
    build: .
    container_name: helping-yourself-app
    ports:
      - "8080:8080"
    env_file :
      - .env
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://helpingyourself.c5gs4246owpo.af-south-1.rds.amazonaws.com:3306/helpingyourself?useSSL=true&serverTimezone=UTC&createDatabaseIfNotExist=true
      - SPRING_DATASOURCE_USERNAME=admin
      - SPRING_DATASOURCE_PASSWORD=Mkha2025!Rds#
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
      - SPRING_PROFILES_ACTIVE=prod
      - AWS_S3_REGION=af-south-1
      - JWT_SECRET=${JWT_SECRET}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - SPRING_MAIL_HOST=email-smtp.us-east-1.amazonaws.com
      - SPRING_MAIL_PORT=587
      - SPRING_MAIL_USERNAME=AKIAU3YFN5RRYS6HY4BJ
      - SPRING_MAIL_PASSWORD=+LaVJnJaI5Tnk05DXEHCLc6cDQ2Qi/aIPowkSfZbbyk=
      - APP_EMAIL_FROM=no-reply@hysinternational.com

    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped

    # Santé de l'app
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8080/actuator/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
